{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build OpenGL Project",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "-std=c++17",
                "${workspaceFolder}/src/*.cpp",
                "${workspaceFolder}/src/core/*.cpp",
                "${workspaceFolder}/src/imgui/*.cpp",
                "${workspaceFolder}/src/glad.c",
                "-I${workspaceFolder}/include",
                "-I${workspaceFolder}/src",
                "-I${workspaceFolder}/src/imgui",
                "-I${workspaceFolder}/vcpkg/installed/x64-mingw-dynamic/include",
                "-L${workspaceFolder}/lib",
                "-L${workspaceFolder}/vcpkg/installed/x64-mingw-dynamic/lib",
                "-lglfw3dll",
                "-lassimp",
                "-lopengl32",
                "-lgdi32",
                "-limm32",
                "-o",
                "${workspaceFolder}/main.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "PATH": "${workspaceFolder}/vcpkg/installed/x64-mingw-dynamic/bin;${env:PATH}"
                }
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiler task for full OpenGL Project with ImGui"
        }
    ]
}